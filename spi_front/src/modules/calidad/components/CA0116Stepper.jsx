import React from "react";
import { FiCheck, FiActivity, FiX } from "react-icons/fi";
const steps = [{ k: "sampling", l: "Muestreo" }, { k: "analyzing", l: "Analizando" }, { k: "approved", l: "Aprobado" }, { k: "released", l: "Liberado" }];
export default function CA0116Stepper({ record, onClose }) {
  const active = steps.findIndex(s => s.k === (record?.status || "sampling"));
  return (<div className="flex flex-col h-full"><div className="flex items-center justify-between px-4 py-3 border-b border-gray-200"><h3 className="font-semibold text-gray-900">Flujo de Muestreo</h3><button onClick={onClose} className="p-1 text-gray-400 hover:text-gray-600 rounded"><FiX className="w-5 h-5" /></button></div><div className="flex-1 p-4"><div className="flex items-center justify-between mb-6">{steps.map((s, i) => (<React.Fragment key={s.k}><div className="flex flex-col items-center"><div className={`w-10 h-10 rounded-full flex items-center justify-center ${i <= active ? "bg-blue-600 text-white" : "bg-gray-100 text-gray-400"}`}>{i < active ? <FiCheck className="w-5 h-5" /> : <FiActivity className="w-5 h-5" />}</div><span className={`text-xs font-medium mt-2 ${i <= active ? "text-gray-900" : "text-gray-400"}`}>{s.l}</span></div>{i < steps.length - 1 && <div className={`flex-1 h-0.5 mx-2 ${i < active ? "bg-blue-600" : "bg-gray-200"}`} />}</React.Fragment>))}</div></div><div className="flex items-center justify-end px-4 py-3 border-t border-gray-200 bg-gray-50"><button onClick={onClose} className="px-4 py-2 text-sm bg-blue-600 text-white rounded-md hover:bg-blue-700">Cerrar</button></div></div>);
}